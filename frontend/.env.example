# 开发环境变量示例（可复制为 .env.local 覆盖）
# 后端代理目标：vite.config.ts 读取，默认 http://localhost:8080
VITE_PROXY_TARGET=http://localhost:8080

# WebSocket 地址：留空则按当前页面同源推导（开发由 Vite 代理 /ws 到后端），
# 仅在前后端不同域部署时才需要显式指定，例如 wss://api.example.com/ws/chat
VITE_WS_URL=
